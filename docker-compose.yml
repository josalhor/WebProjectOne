version: '3.6'

services:
  web:
    build: ./
    command: python /app/src/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/app/
    ports:
      - 8000:8000
    # TODO:
    # env_file:
    #   - ./.env.dev